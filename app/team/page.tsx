"use client";
import { useEffect, useState } from "react";
import { Users, ShieldCheck, Mail, Plus } from "lucide-react";
import AppShell from "@/components/AppShell";

type Data={user:{name:string;email:string};organization:{name:string;role:string}};
export default function TeamPage(){const[data,setData]=useState<Data|null>(null);useEffect(()=>{fetch("/api/dashboard").then(r=>r.json()).then(setData)},[]);return <AppShell user={data?.user} organization={data?.organization}><main className="min-h-screen px-6 py-8 lg:px-10"><div className="mx-auto max-w-5xl"><div className="flex items-end justify-between gap-4"><div><p className="text-xs uppercase tracking-[.18em] text-violet-300">Workspace / Team</p><h1 className="mt-2 text-3xl font-semibold">Your team</h1><p className="mt-2 text-sm text-zinc-500">Manage workspace members and access levels.</p></div><button className="flex items-center gap-2 rounded-xl bg-violet-500 px-4 py-2.5 text-sm font-semibold"><Plus size={16}/> Invite member</button></div><section className="mt-8 rounded-3xl border border-white/[.08] bg-white/[.025] p-5"><div className="flex items-center gap-3 border-b border-white/[.06] pb-5"><div className="grid h-10 w-10 place-items-center rounded-xl bg-violet-500/10"><Users size={18} className="text-violet-300"/></div><div><p className="font-medium">{data?.organization.name||"Workspace"}</p><p className="text-xs text-zinc-600">Workspace members</p></div></div>{data&&<div className="mt-3 flex items-center gap-4 rounded-2xl p-4 hover:bg-white/[.02]"><div className="grid h-10 w-10 place-items-center rounded-full bg-gradient-to-br from-violet-500 to-fuchsia-500 text-xs font-bold">{data.user.name.split(" ").map(x=>x[0]).join("").slice(0,2).toUpperCase()}</div><div className="min-w-0 flex-1"><p className="text-sm font-medium">{data.user.name}</p><p className="mt-1 flex items-center gap-1 text-xs text-zinc-600"><Mail size={12}/>{data.user.email}</p></div><span className="flex items-center gap-1 rounded-full bg-emerald-400/10 px-3 py-1 text-[11px] text-emerald-300"><ShieldCheck size={12}/>{data.organization.role}</span></div>}</section></div></main></AppShell>}
