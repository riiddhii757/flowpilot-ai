import { NextResponse } from "next/server";
import { db } from "@/lib/db";
import { encrypt, googleRedirectUri, verifyOAuthState } from "@/lib/oauth";
export async function GET(request:Request){const url=new URL(request.url);const code=url.searchParams.get("code");const rawState=url.searchParams.get("state")||"";const [provider,state]=rawState.split(":",2);const userId=verifyOAuthState(state||null);if(!code||!userId||(provider!=="calendar"&&provider!=="gmail"))return NextResponse.redirect(new URL("/integrations?error=google_oauth",request.url));const clientId=process.env.GOOGLE_CLIENT_ID,clientSecret=process.env.GOOGLE_CLIENT_SECRET;if(!clientId||!clientSecret)return NextResponse.redirect(new URL("/integrations?error=google_config",request.url));const tokenResponse=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams({code,client_id:clientId,client_secret:clientSecret,redirect_uri:googleRedirectUri(request),grant_type:"authorization_code"})});if(!tokenResponse.ok)return NextResponse.redirect(new URL("/integrations?error=google_token",request.url));const tokens=await tokenResponse.json() as {access_token:string;refresh_token?:string;expires_in?:number};const profileResponse=await fetch("https://openidconnect.googleapis.com/v1/userinfo",{headers:{Authorization:`Bearer ${tokens.access_token}`}});const profile=profileResponse.ok?await profileResponse.json() as {email?:string}:{};const user=await db.user.findUnique({where:{id:userId},include:{members:true}});if(!user||!user.members[0])return NextResponse.redirect(new URL("/integrations?error=organization",request.url));const existing=await db.integration.findUnique({where:{organizationId_provider:{organizationId:user.members[0].organizationId,provider:`google-${provider}`}}});await db.integration.upsert({where:{organizationId_provider:{organizationId:user.members[0].organizationId,provider:`google-${provider}`}},create:{userId,organizationId:user.members[0].organizationId,provider:`google-${provider}`,enabled:true,accountEmail:profile.email||user.email,accessToken:encrypt(tokens.access_token),refreshToken:tokens.refresh_token?encrypt(tokens.refresh_token):(existing?.refreshToken||null),tokenExpiresAt:tokens.expires_in?new Date(Date.now()+tokens.expires_in*1000):null},update:{enabled:true,accountEmail:profile.email||user.email,accessToken:encrypt(tokens.access_token),refreshToken:tokens.refresh_token?encrypt(tokens.refresh_token):(existing?.refreshToken||null),tokenExpiresAt:tokens.expires_in?new Date(Date.now()+tokens.expires_in*1000):null}});return NextResponse.redirect(new URL(`/integrations?connected=${provider}`,request.url));}
