import { NextResponse } from "next/server";
import { db } from "@/lib/db";
import { decrypt, encrypt } from "@/lib/oauth";
import { getCurrentUser } from "@/lib/auth";
async function tokenFor(organizationId:string){let row=await db.integration.findUnique({where:{organizationId_provider:{organizationId,provider:"google-calendar"}}});if(!row?.refreshToken)return null;if(row.tokenExpiresAt&&row.tokenExpiresAt.getTime()>Date.now()+60000)return row.accessToken?decrypt(row.accessToken):null;const clientId=process.env.GOOGLE_CLIENT_ID,clientSecret=process.env.GOOGLE_CLIENT_SECRET;if(!clientId||!clientSecret)return null;const response=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams({client_id:clientId,client_secret:clientSecret,refresh_token:decrypt(row.refreshToken),grant_type:"refresh_token"})});if(!response.ok)return null;const data=await response.json() as {access_token:string;expires_in?:number};row=await db.integration.update({where:{id:row.id},data:{accessToken:encrypt(data.access_token),tokenExpiresAt:data.expires_in?new Date(Date.now()+data.expires_in*1000):null}});return data.access_token;}
export async function POST(request:Request){const user=await getCurrentUser();if(!user||!user.members[0])return NextResponse.json({error:"Authentication required"},{status:401});const body=await request.json() as {summary?:string;description?:string;start?:string;end?:string;attendees?:string[]};if(!body.summary||!body.start||!body.end)return NextResponse.json({error:"summary, start and end are required"},{status:400});const accessToken=await tokenFor(user.members[0].organizationId);if(!accessToken)return NextResponse.json({error:"Google Calendar is not connected"},{status:409});const response=await fetch("https://www.googleapis.com/calendar/v3/calendars/primary/events",{method:"POST",headers:{Authorization:`Bearer ${accessToken}`,"Content-Type":"application/json"},body:JSON.stringify({summary:body.summary,description:body.description,start:{dateTime:body.start},end:{dateTime:body.end},attendees:(body.attendees||[]).map(email=>({email}))})});const data=await response.json();if(!response.ok)return NextResponse.json({error:data.error?.message||"Google Calendar request failed"},{status:response.status});return NextResponse.json({ok:true,event:{id:data.id,htmlLink:data.htmlLink,summary:data.summary,start:data.start,end:data.end}});}
