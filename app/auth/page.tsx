"use client";
import {FormEvent,useState} from "react";import {ArrowRight,Bot,Eye,EyeOff,LockKeyhole,Sparkles,Zap,ShieldCheck} from "lucide-react";import {useSearchParams} from "next/navigation";
export const dynamic="force-dynamic";
export default function AuthPage(){const params=useSearchParams();const initial=params.get("mode")==="login"?"login":"signup";const[mode,setMode]=useState<"signup"|"login">(initial),[showPassword,setShowPassword]=useState(false),[form,setForm]=useState({name:"",email:"",password:"",workspace:""}),[error,setError]=useState(""),[loading,setLoading]=useState(false);function switchMode(next:"signup"|"login"){setMode(next);setError("");setShowPassword(false);window.history.replaceState(null,"",`/auth?mode=${next}`)}async function submit(e:FormEvent){e.preventDefault();setError("");setLoading(true);try{const endpoint=mode==="signup"?"/api/auth/register":"/api/auth/login";const res=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const data=await res.json().catch(()=>({error:"Server returned an invalid response"}));if(!res.ok){setError(data.error||"Something went wrong");setLoading(false);return}window.location.href="/"}catch{setError("Unable to connect to FlowPilot. Please try again.");setLoading(false)}}return <main className="relative min-h-screen overflow-hidden bg-[#070812] text-white"><div className="absolute inset-0 bg-[url('/flowpilot-wallpaper.svg')] bg-cover bg-center"/><div className="absolute inset-0 bg-gradient-to-br from-[#070812]/70 via-violet-950/35 to-cyan-950/45"/><div className="relative mx-auto grid min-h-screen w-full max-w-6xl items-center gap-12 p-6 lg:grid-cols-[1fr_450px] lg:p-10"><section className="hidden lg:block"><div className="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-4 py-2 text-xs text-violet-100 backdrop-blur"><Sparkles size={14}/> Intelligent operations, beautifully simple</div><h1 className="mt-7 max-w-2xl text-6xl font-semibold leading-[1.05] tracking-tight">Welcome to <span className="bg-gradient-to-r from-violet-200 via-fuchsia-200 to-cyan-200 bg-clip-text text-transparent">FlowPilot.</span></h1><p className="mt-6 max-w-xl text-lg leading-8 text-zinc-300">Your AI operations cockpit for turning repetitive work into reliable, measurable automations.</p><div className="mt-9 grid max-w-xl grid-cols-3 gap-3">{[[Zap,"Automate","Build workflows"],[Bot,"Ask AI","Design with Gemini"],[ShieldCheck,"Control","Roles + audit"]].map(([Icon,title,sub])=><div key={title as string} className="rounded-2xl border border-white/10 bg-white/[.07] p-4 backdrop-blur"><Icon className="text-cyan-300" size={19}/><p className="mt-3 text-sm font-semibold">{title as string}</p><p className="mt-1 text-xs text-zinc-400">{sub as string}</p></div>)}</div></section><section><div className="mb-5 flex items-center gap-3 lg:hidden"><div className="rounded-2xl bg-gradient-to-br from-violet-500 to-cyan-400 p-3"><Bot size={22}/></div><div><div className="text-xl font-semibold">FlowPilot</div><div className="text-xs text-zinc-400">AI operations automation</div></div></div><div className="rounded-[2rem] border border-white/15 bg-[#0c0d18]/75 p-7 shadow-2xl shadow-black/50 backdrop-blur-2xl"><div className="flex items-center gap-3"><div className="rounded-2xl bg-gradient-to-br from-violet-500 to-cyan-400 p-3"><Bot size={22}/></div><div><div className="text-xl font-semibold">FlowPilot</div><div className="text-xs text-zinc-400">AI operations automation</div></div></div><h2 className="mt-7 text-2xl font-semibold">{mode==="signup"?"Create your workspace":"Welcome back"}</h2><p className="mt-2 text-sm leading-6 text-zinc-400">{mode==="signup"?"Start your intelligent operations workspace in seconds.":"Pick up where your automations left off."}</p><form onSubmit={submit} className="mt-7 space-y-4">{mode==="signup"&&<><label className="block text-sm">Your name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Riddhi Thakkar" className="mt-2 w-full rounded-xl border border-white/10 bg-white/[.05] px-4 py-3.5 outline-none focus:border-violet-400"/></label><label className="block text-sm">Workspace name<input required value={form.workspace} onChange={e=>setForm({...form,workspace:e.target.value})} placeholder="Riddhi Operations" className="mt-2 w-full rounded-xl border border-white/10 bg-white/[.05] px-4 py-3.5 outline-none focus:border-violet-400"/></label></>}<label className="block text-sm">Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com" className="mt-2 w-full rounded-xl border border-white/10 bg-white/[.05] px-4 py-3.5 outline-none focus:border-violet-400"/></label><label className="block text-sm">Password<div className="relative mt-2"><input required minLength={8} type={showPassword?"text":"password"} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="At least 8 characters" className="w-full rounded-xl border border-white/10 bg-white/[.05] px-4 py-3.5 pr-12 outline-none focus:border-violet-400"/><button type="button" onClick={()=>setShowPassword(v=>!v)} className="absolute right-2 top-1/2 -translate-y-1/2 rounded-lg p-2 text-zinc-400 hover:text-white">{showPassword?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{mode==="login"&&<button type="button" onClick={()=>setError("Password reset is not configured yet. We will add secure email reset in the next auth phase.")} className="flex items-center gap-1.5 text-xs text-cyan-300"><LockKeyhole size={13}/> Forgot password?</button>}{error&&<p className="rounded-xl border border-red-400/20 bg-red-400/10 p-3 text-sm text-red-200">{error}</p>}<button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-violet-500 via-fuchsia-500 to-cyan-400 py-3.5 font-semibold">{loading?"Connecting...":mode==="signup"?"Create workspace":"Sign in"}<ArrowRight size={16}/></button></form><button type="button" onClick={()=>switchMode(mode==="signup"?"login":"signup")} className="mt-6 w-full text-sm text-zinc-400 hover:text-white">{mode==="signup"?"Already have an account? Sign in":"New to FlowPilot? Create a workspace"}</button></div></section></div></main>}
