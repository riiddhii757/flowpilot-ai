"use client";
import { FormEvent, useState } from "react";
import { ArrowRight, Bot, Eye, EyeOff, LockKeyhole, Sparkles } from "lucide-react";

export default function AuthPage(){
  const [mode,setMode]=useState<"signup"|"login">("signup");
  const [showPassword,setShowPassword]=useState(false);
  const [form,setForm]=useState({name:"",email:"",password:"",workspace:""});
  const [error,setError]=useState(""); const [loading,setLoading]=useState(false);
  async function submit(e:FormEvent){e.preventDefault();setError("");setLoading(true);try{const endpoint=mode==="signup"?"/api/auth/register":"/api/auth/login";const res=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const data=await res.json().catch(()=>({error:"Server returned an invalid response"}));if(!res.ok){setError(data.error||"Something went wrong");setLoading(false);return}window.location.href="/"}catch{setError("Unable to connect to the server. Please try again.");setLoading(false)}}
  return <main className="relative min-h-screen overflow-hidden bg-[#070812] text-white flex items-center justify-center p-6"><div className="pointer-events-none absolute inset-0"><div className="absolute -left-32 -top-32 h-96 w-96 rounded-full bg-violet-600/20 blur-3xl"/><div className="absolute -bottom-40 -right-20 h-[28rem] w-[28rem] rounded-full bg-fuchsia-500/15 blur-3xl"/><div className="absolute left-1/2 top-1/3 h-72 w-72 -translate-x-1/2 rounded-full bg-cyan-400/10 blur-3xl"/></div><div className="relative grid w-full max-w-5xl gap-10 lg:grid-cols-[1fr_440px] lg:items-center"><div className="hidden lg:block"><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-violet-300/20 bg-violet-300/10 px-3 py-1.5 text-xs text-violet-200"><Sparkles size={13}/> AI operations, beautifully simple</div><h1 className="max-w-xl text-5xl font-semibold tracking-tight">Welcome to <span className="bg-gradient-to-r from-violet-300 via-fuchsia-300 to-cyan-300 bg-clip-text text-transparent">FlowPilot.</span></h1><p className="mt-5 max-w-lg text-lg leading-8 text-zinc-400">Turn repetitive work into intelligent automations your team can trust.</p><div className="mt-8 grid max-w-lg gap-3 sm:grid-cols-3">{[["AI-first","Workflow design"],["Secure","Role-based access"],["Live","Operational visibility"]].map(([a,b])=><div key={a} className="rounded-2xl border border-white/10 bg-white/[.04] p-4"><p className="text-sm font-semibold">{a}</p><p className="mt-1 text-xs text-zinc-500">{b}</p></div>)}</div></div><div><div className="mb-6 flex items-center gap-3"><div className="rounded-2xl bg-gradient-to-br from-violet-500 to-fuchsia-500 p-3 shadow-lg shadow-violet-900/30"><Bot size={22}/></div><div><div className="text-xl font-semibold">FlowPilot</div><div className="text-xs text-zinc-500">AI operations automation</div></div></div><div className="rounded-[2rem] border border-white/10 bg-white/[.045] p-7 shadow-2xl shadow-black/40 backdrop-blur-xl"><h1 className="text-2xl font-semibold">{mode==="signup"?"Create your workspace":"Welcome back"}</h1><p className="mt-2 text-sm text-zinc-500">{mode==="signup"?"Start automating your team's busywork.":"Sign in to continue to FlowPilot."}</p><form onSubmit={submit} className="mt-7 space-y-4">{mode==="signup"&&<><label className="block text-sm">Your name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Riddhi Thakkar" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3.5 outline-none transition focus:border-violet-400/70"/></label><label className="block text-sm">Workspace name<input required value={form.workspace} onChange={e=>setForm({...form,workspace:e.target.value})} placeholder="Riddhi Operations" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3.5 outline-none transition focus:border-violet-400/70"/></label></>}<label className="block text-sm">Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3.5 outline-none transition focus:border-violet-400/70"/></label><label className="block text-sm">Password<div className="relative mt-2"><input required minLength={8} type={showPassword?"text":"password"} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="At least 8 characters" className="w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3.5 pr-12 outline-none transition focus:border-violet-400/70"/><button type="button" onClick={()=>setShowPassword(v=>!v)} className="absolute right-2 top-1/2 -translate-y-1/2 rounded-lg p-2 text-zinc-500 hover:text-white">{showPassword?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{mode==="login"&&<button type="button" onClick={()=>setError("Password reset is not configured yet. Please contact your workspace administrator.")} className="flex items-center gap-1.5 text-xs text-violet-300"><LockKeyhole size={13}/> Forgot password?</button>}{error&&<p className="rounded-xl border border-red-400/20 bg-red-400/10 p-3 text-sm text-red-300">{error}</p>}<button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-violet-500 to-fuchsia-500 py-3.5 font-semibold shadow-lg">{loading?"Working...":mode==="signup"?"Create workspace":"Sign in"}<ArrowRight size={16}/></button></form><button onClick={()=>{setMode(mode==="signup"?"login":"signup");setError("");setShowPassword(false)}} className="mt-6 w-full text-sm text-zinc-400 hover:text-white">{mode==="signup"?"Already have an account? Sign in":"New to FlowPilot? Create a workspace"}</button></div></div></div></main>;
}