"use client";
import {useState} from "react";
import {CheckCircle2,Clock3,ShieldAlert,X} from "lucide-react";
const initial=[{id:"t-104",title:"Refund request from Acme",reason:"Refund is above the $500 approval threshold.",amount:"$840.00",requester:"AI Support Agent"},{id:"t-105",title:"Upgrade enterprise account",reason:"Contract value requires account-owner approval.",amount:"$12,000 ARR",requester:"AI Sales Agent"},{id:"t-106",title:"Invoice credit adjustment",reason:"Finance policy requires review for credits over $250.",amount:"$430.00",requester:"AI Finance Agent"}];
export default function Approvals(){const[items,setItems]=useState(initial);const[done,setDone]=useState<string[]>([]);async function decide(id:string,decision:"approve"|"reject"){await fetch("/api/approvals",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({taskId:id,decision})});setDone(x=>[...x,id]);setTimeout(()=>setItems(x=>x.filter(i=>i.id!==id)),300)}return <main className="min-h-screen bg-[#08090d] text-white"><header className="border-b border-white/10 px-6 py-6 lg:px-10"><p className="text-sm text-zinc-500">Operations / Approvals</p><h1 className="mt-1 text-2xl font-semibold">Approval queue</h1><p className="mt-2 text-sm text-zinc-500">Review sensitive AI actions before FlowPilot executes them.</p></header><div className="mx-auto max-w-4xl space-y-4 p-6 lg:p-10">{items.map(i=><article key={i.id} className="rounded-2xl border border-white/10 bg-white/[.03] p-6"><div className="flex items-start justify-between gap-4"><div className="flex gap-3"><div className="rounded-xl bg-amber-400/10 p-2.5 text-amber-300"><ShieldAlert size={18}/></div><div><h2 className="font-semibold">{i.title}</h2><p className="mt-1 text-xs text-zinc-500">Requested by {i.requester} · <Clock3 className="inline" size={12}/> just now</p></div></div><span className="rounded-full bg-amber-400/10 px-3 py-1 text-xs text-amber-300">Needs review</span></div><div className="mt-5 rounded-xl border border-white/10 bg-black/20 p-4"><p className="text-sm text-zinc-300">{i.reason}</p><p className="mt-3 text-xl font-semibold">{i.amount}</p></div><div className="mt-5 flex gap-2"><button onClick={()=>decide(i.id,"reject")} className="flex items-center gap-2 rounded-xl border border-white/10 px-4 py-2.5 text-sm"><X size={15}/> Reject</button><button onClick={()=>decide(i.id,"approve")} className="flex items-center gap-2 rounded-xl bg-emerald-500 px-4 py-2.5 text-sm font-semibold text-black"><CheckCircle2 size={15}/> Approve action</button></div></article>)}{items.length===0&&<div className="rounded-2xl border border-white/10 p-10 text-center"><CheckCircle2 className="mx-auto text-emerald-400"/><p className="mt-3 font-medium">All approvals are handled.</p><p className="mt-1 text-sm text-zinc-500">Your AI agents are back to work.</p></div>}</div></main>}
