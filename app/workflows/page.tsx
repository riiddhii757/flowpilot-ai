"use client";
import {useState} from "react";
import {ArrowRight,Check,Clock3,Plus,Save,Trash2,Zap} from "lucide-react";
const starters=[{id:"trigger",label:"When a request arrives",detail:"Inbox / webhook"},{id:"ai",label:"AI triage request",detail:"Classify + summarize"},{id:"knowledge",label:"Check company policy",detail:"Knowledge base"},{id:"approval",label:"Ask for approval",detail:"Only when required"},{id:"action",label:"Execute action",detail:"Send email / CRM"}];
export default function Workflows(){const[steps,setSteps]=useState(starters);const[saved,setSaved]=useState(false);function remove(id:string){if(id!=="trigger")setSteps(s=>s.filter(x=>x.id!==id))}return <main className="min-h-screen bg-[#08090d] text-white"><header className="flex items-center justify-between border-b border-white/10 px-6 py-5 lg:px-10"><div><p className="text-sm text-zinc-500">Automations / New workflow</p><h1 className="mt-1 text-xl font-semibold">Support request automation</h1></div><div className="flex gap-2"><button className="rounded-xl border border-white/10 px-4 py-2 text-sm">Test</button><button onClick={()=>{setSaved(true);setTimeout(()=>setSaved(false),1800)}} className="flex items-center gap-2 rounded-xl bg-violet-500 px-4 py-2 text-sm font-semibold"><Save size={15}/>{saved?"Saved":"Save workflow"}</button></div></header><div className="mx-auto grid max-w-6xl gap-6 p-6 lg:grid-cols-[1fr_300px] lg:p-10"><section className="rounded-2xl border border-white/10 bg-white/[.03] p-6"><div className="mb-6 flex items-center justify-between"><div><h2 className="font-semibold">Workflow builder</h2><p className="mt-1 text-sm text-zinc-500">Connect business steps. FlowPilot handles execution.</p></div><span className="rounded-full bg-emerald-400/10 px-3 py-1 text-xs text-emerald-300">Draft</span></div><div className="space-y-3">{steps.map((s,i)=><div key={s.id}><div className="flex items-center gap-4 rounded-2xl border border-white/10 bg-black/20 p-4"><div className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-violet-500/15 text-violet-300">{i===0?<Zap size={16}/>:i===1?<span className="text-xs font-bold">AI</span>:i===2?<span>⌕</span>:i===3?<Clock3 size={16}/>:<Check size={16}/>}</div><div className="flex-1"><p className="text-sm font-medium">{s.label}</p><p className="mt-1 text-xs text-zinc-500">{s.detail}</p></div>{i>0&&<button onClick={()=>remove(s.id)} className="text-zinc-600 hover:text-red-300"><Trash2 size={15}/></button>}</div>{i<steps.length-1&&<div className="ml-8 h-3 border-l border-dashed border-zinc-700"/>}</div>)}</div><button onClick={()=>setSteps(s=>[...s,{id:`step-${Date.now()}`,label:"New action",detail:"Configure an integration"}])} className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl border border-dashed border-white/15 py-3 text-sm text-zinc-400 hover:bg-white/5"><Plus size={15}/> Add step</button></section><aside className="space-y-4"><div className="rounded-2xl border border-white/10 bg-white/[.03] p-5"><h3 className="font-semibold">Workflow settings</h3><label className="mt-5 block text-xs text-zinc-500">TRIGGER</label><div className="mt-2 rounded-xl border border-white/10 p-3 text-sm">Inbox receives a request</div><label className="mt-5 block text-xs text-zinc-500">RUN MODE</label><div className="mt-2 rounded-xl border border-white/10 p-3 text-sm">Automatic + approval</div></div><div className="rounded-2xl border border-violet-400/15 bg-violet-500/[.05] p-5"><p className="text-xs font-medium text-violet-300">AI SUGGESTION</p><p className="mt-3 text-sm leading-6 text-zinc-300">Add a knowledge check before the approval step so agents can answer routine policy questions automatically.</p><button className="mt-4 flex items-center gap-1 text-sm text-violet-300">Apply suggestion <ArrowRight size={14}/></button></div></aside></div></main>}
