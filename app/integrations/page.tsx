"use client";
import {useState} from "react";
import {Check,Mail,MessageSquare,Plug,Webhook} from "lucide-react";
const providers=[{id:"slack",name:"Slack",desc:"Send alerts and approval requests to your team.",icon:MessageSquare},{id:"email",name:"Email",desc:"Trigger workflows from shared inboxes and send replies.",icon:Mail},{id:"webhook",name:"Webhooks",desc:"Connect any system that can send HTTP events.",icon:Webhook},{id:"crm",name:"CRM",desc:"Sync leads, accounts and customer actions.",icon:Plug}];
export default function Integrations(){const[state,setState]=useState<Record<string,boolean>>({email:true,webhook:true});async function toggle(id:string){const next=!state[id];setState(s=>({...s,[id]:next}));await fetch("/api/integrations",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({provider:id,enabled:next})})}return <main className="min-h-screen bg-[#08090d] text-white"><header className="border-b border-white/10 px-6 py-6 lg:px-10"><p className="text-sm text-zinc-500">Workspace / Integrations</p><h1 className="mt-1 text-2xl font-semibold">Connect your tools</h1><p className="mt-2 text-sm text-zinc-500">Give FlowPilot the context and actions it needs to automate work.</p></header><div className="mx-auto max-w-4xl space-y-3 p-6 lg:p-10">{providers.map(p=>{const I=p.icon;const on=!!state[p.id];return <div key={p.id} className="flex items-center gap-4 rounded-2xl border border-white/10 bg-white/[.03] p-5"><div className="rounded-xl bg-white/5 p-3"><I size={20}/></div><div className="flex-1"><h2 className="font-medium">{p.name}</h2><p className="mt-1 text-sm text-zinc-500">{p.desc}</p></div><button onClick={()=>toggle(p.id)} className={`flex items-center gap-2 rounded-xl px-4 py-2 text-sm font-medium ${on?"bg-emerald-500 text-black":"border border-white/10 text-zinc-300"}`}>{on&&<Check size={15}/>} {on?"Connected":"Connect"}</button></div>})}</div></main>}
